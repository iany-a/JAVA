

"sout" - print

"psvm" - main function



"Main" class = entry point class which has method public static void main (psvm) as entry point







4th visibility: "package" - this is the default visibility in JAVA

csie.ase.ro/en

package is the URL in reverse:

en.ase.csie



NOTE: use .length to get an array size



in JAVA the root is always object. All classes stem from it.

in JAVA:

overload: change type, no, order

name( arg list )



override: only works in inheritance

@Override tells the compiler that function exists, and implementation needs to be changed
in object we have the clone() method




